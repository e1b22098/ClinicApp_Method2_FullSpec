<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unknownclinic.appointment.repository.TimeSlotMapper">

    <resultMap id="TimeSlotResultMap" type="com.unknownclinic.appointment.domain.TimeSlot">
        <id property="id" column="id"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="isActive" column="is_active"/>
    </resultMap>

    <select id="findAllActive" resultMap="TimeSlotResultMap">
        SELECT id, start_time, end_time, is_active
        FROM time_slots
        WHERE is_active = TRUE
        ORDER BY start_time ASC
    </select>

    <select id="findById" resultMap="TimeSlotResultMap">
        SELECT id, start_time, end_time, is_active
        FROM time_slots
        WHERE id = #{id}
    </select>

</mapper>

